#include <iostream>
#include <string>
#include <algorithm>
using namespace std;
int main()
{
    int n;
    int a, b;
    cin >> n;
    cin >> a >> b;
    int k;
    int count = 0;
    if (a == b)
    {
        count = 0;
        cout << count << endl;
    }
    else if (a < b)
    {
        a = a + k;
    }
    else
    {
        a = a - k;
    }
}